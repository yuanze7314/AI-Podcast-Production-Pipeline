/**
 * ============================================================================
 * Phase 1 运行脚本
 * ============================================================================
 *
 * 功能概述：
 * 本脚本用于独立运行 Phase 1（数据层 - 原子化）。
 *
 * ============================================================================
 * 使用方法：
 * ============================================================================
 *
 * 在 package.json 中已配置脚本命令：
 *
 *   npm run phase1
 *
 * 等效于直接运行：
 *
 *   node src/run-phase1.js
 *
 * ============================================================================
 * 执行内容：
 * ============================================================================
 *
 * 1. 分析读者评论，生成 metadata.json
 * 2. 解析PDF并按章节切分
 * 3. 为每章提取逻辑原子和金句
 *
 * ============================================================================
 * 前置条件：
 * ============================================================================
 *
 * 1. 已配置 .env 文件
 * 2. 准备好书籍PDF、评论文件、目录文件
 * 3. 已安装依赖：npm install
 *
 * ============================================================================
 * 输出文件：
 * ============================================================================
 *
 * - output/config/metadata.json    # 播客定调元数据
 * - temp/raw_chapters/raw_ch*.txt # 原始章节文本
 * - temp/processed/chapter_*.json # 章节档案
 *
 * @module run-phase1
 */

import 'dotenv/config';
import { runPhase1 } from './orchestrate.js';

// 运行 Phase 1
runPhase1()
  .then(() => {
    // 正常退出
    process.exit(0);
  })
  .catch(err => {
    // 错误处理
    console.error(err);
    process.exit(1);
  });
